var sharePizza = parseFloat(cutPizzaSlices(8, 2)).toFixed(2);

function cutPizzaSlices(a, b) {
  return a / b;
}
console.log(`Each person gets ${sharePizza} slices of pizza.`);

var sharePizza = parseFloat(cutPizzaSlices(8, 3)).toFixed(2);

function cutPizzaSlices(a, b) {
  return a / b;
}

console.log(`Each person gets ${sharePizza} slices of pizza.`)